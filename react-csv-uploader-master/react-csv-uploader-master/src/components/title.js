import React from "react";

export default () => (
  <div className="title">
    <h5 className="title-head">React CSV Uploader</h5>
    <p className="title-sub">
      A simple app to upload csv and check csv updates
    </p>
  </div>
);
